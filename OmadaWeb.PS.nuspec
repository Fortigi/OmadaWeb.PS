<?xml version="1.0"?>
<package>
    <metadata>
        <id>OmadaWeb.PS</id>
        <version>$VERSION$</version>
        <authors>Fortigi</authors>
        <description>Module containing PowerShell commands to manage data via Omada web and OData endpoints in the cloud or on-prem. This module adds support for additional authentication types like OAuth2 based on client credentials and browser based login.</description>
        <copyright>Copyright $YEAR$ Fortigi</copyright>
    </metadata>
    <files>
        <file src="buildoutput\OmadaWeb.PS\OmadaWeb.PS.psd1" target="" />
        <file src="buildoutput\OmadaWeb.PS\OmadaWeb.PS.psm1" target="" />
        <file src="buildoutput\OmadaWeb.PS\OmadaWeb.PS.nuspec" target="" />
        <!-- Pinned versions and SHA-256 hashes of the binaries the module downloads at runtime.
             Without it every download fails closed, so it has to ship with the module. -->
        <file src="buildoutput\OmadaWeb.PS\DependencyLock.psd1" target="" />
        <!-- The WebView2 SDK assemblies, fetched and hash-verified at build time by
             Build\Get-BundledDependency.ps1. Shipping them is what lets the default
             -AuthenticationType WebView2 work without egress to nuget.org, and keeps them out of a
             user-writable directory. -->
        <file src="buildoutput\OmadaWeb.PS\lib\**" target="lib" />
        <file src="buildoutput\OmadaWeb.PS\ThirdPartyNotices.txt" target="" />
    </files>
</package>